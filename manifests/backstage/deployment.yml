apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: backstage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backstage
  template:
    metadata:
      labels:
        app: backstage
    spec:
          imagePullSecrets:
            - name: acr-secret
          containers:
            - name: backstage
              image: arodemoregistry.azurecr.io/backstage:313fc6c5194a94a2d4d37a7b64e4ce31068ff021
              ports:
                - containerPort: 7007