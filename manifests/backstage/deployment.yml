apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: backstage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backstage
  template:
    metadata:
      labels:
        app: backstage
    spec:
          imagePullSecrets:
            - name: acr-secret
          containers:
            - name: backstage
              image: arodemoregistry.azurecr.io/backstage:62c23cc703c365c00b4b2c682b92a345cdd0c144
              ports:
                - containerPort: 7007